apiVersion: apps/v1
kind: Deployment
metadata:
  name: mydocctor-auth-deployment
  labels:
    name:  auth-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-service
  template:
    metadata:
      labels:
        app: auth-service
    spec:
      containers:
      - image: arjun164/mydocctor-auth:latest
        name: auth-service
        envFrom:
          - configMapRef:
              name: auth-service-config